//
// CoEdgeappServerAiUserUserAvatarGeneratorInput.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CoEdgeappServerAiUserUserAvatarGeneratorInput: Codable, JSONEncodable, Hashable {

    public enum Style: String, Codable, CaseIterable {
        case _none = "NONE"
        case retro = "RETRO"
        case futuristic = "FUTURISTIC"
        case minimalist = "MINIMALIST"
        case cartoon = "CARTOON"
        case natureInspired = "NATURE_INSPIRED"
        case abstractShapes = "ABSTRACT_SHAPES"
        case neonGlow = "NEON_GLOW"
        case vintagePoster = "VINTAGE_POSTER"
        case pixelArt = "PIXEL_ART"
    }
    public enum Palette: String, Codable, CaseIterable {
        case _none = "NONE"
        case bold = "BOLD"
        case pastel = "PASTEL"
        case grayscale = "GRAYSCALE"
        case neon = "NEON"
        case earthy = "EARTHY"
        case retro = "RETRO"
        case cool = "COOL"
        case warm = "WARM"
        case monochrome = "MONOCHROME"
        case metallic = "METALLIC"
    }
    public var userId: String?
    public var description: String?
    public var language: String?
    public var style: Style?
    public var palette: Palette?
    public var deterministic: Bool?
    public var seed: Int64?

    public init(userId: String? = nil, description: String? = nil, language: String? = nil, style: Style? = nil, palette: Palette? = nil, deterministic: Bool? = nil, seed: Int64? = nil) {
        self.userId = userId
        self.description = description
        self.language = language
        self.style = style
        self.palette = palette
        self.deterministic = deterministic
        self.seed = seed
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case userId
        case description
        case language
        case style
        case palette
        case deterministic
        case seed
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(userId, forKey: .userId)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encodeIfPresent(language, forKey: .language)
        try container.encodeIfPresent(style, forKey: .style)
        try container.encodeIfPresent(palette, forKey: .palette)
        try container.encodeIfPresent(deterministic, forKey: .deterministic)
        try container.encodeIfPresent(seed, forKey: .seed)
    }
}

