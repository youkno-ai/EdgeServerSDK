//
// Section.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct Section: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case topPick = "TOP_PICK"
        case individual = "INDIVIDUAL"
        case highlights = "HIGHLIGHTS"
        case uncompleted = "UNCOMPLETED"
        case tokenReward = "TOKEN_REWARD"
        case scheduled = "SCHEDULED"
        case expiration = "EXPIRATION"
        case new = "NEW"
        case adminsChoice = "ADMINS_CHOICE"
        case favourites = "FAVOURITES"
        case hotTopic = "HOT_TOPIC"
        case gem = "GEM"
        case express = "EXPRESS"
        case mood = "MOOD"
        case category = "CATEGORY"
        case all = "ALL"
    }
    public enum CardSize: String, Codable, CaseIterable {
        case small = "SMALL"
        case medium = "MEDIUM"
        case large = "LARGE"
        case extraLarge = "EXTRA_LARGE"
        case fillIn = "FILL_IN"
    }
    public var type: ModelType?
    public var name: String?
    public var cardSize: CardSize?

    public init(type: ModelType? = nil, name: String? = nil, cardSize: CardSize? = nil) {
        self.type = type
        self.name = name
        self.cardSize = cardSize
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case name
        case cardSize
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(type, forKey: .type)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(cardSize, forKey: .cardSize)
    }
}

