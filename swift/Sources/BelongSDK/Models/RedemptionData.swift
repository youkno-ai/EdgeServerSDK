//
// RedemptionData.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct RedemptionData: Codable, JSONEncodable, Hashable {

    public var discountedBy: User?
    public var totalAmount: Reward?
    public var discountAmount: Reward?
    public var discountedAmount: Reward?

    public init(discountedBy: User? = nil, totalAmount: Reward? = nil, discountAmount: Reward? = nil, discountedAmount: Reward? = nil) {
        self.discountedBy = discountedBy
        self.totalAmount = totalAmount
        self.discountAmount = discountAmount
        self.discountedAmount = discountedAmount
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case discountedBy
        case totalAmount
        case discountAmount
        case discountedAmount
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(discountedBy, forKey: .discountedBy)
        try container.encodeIfPresent(totalAmount, forKey: .totalAmount)
        try container.encodeIfPresent(discountAmount, forKey: .discountAmount)
        try container.encodeIfPresent(discountedAmount, forKey: .discountedAmount)
    }
}

