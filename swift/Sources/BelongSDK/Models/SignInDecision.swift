//
// SignInDecision.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct SignInDecision: Codable, JSONEncodable, Hashable {

    public enum NextStep: String, Codable, CaseIterable {
        case _default = "DEFAULT"
        case signup = "SIGNUP"
        case merchantOnboarding = "MERCHANT_ONBOARDING"
        case link = "LINK"
    }
    public enum MissingFields: String, Codable, CaseIterable {
        case name = "NAME"
        case avatar = "AVATAR"
        case gender = "GENDER"
        case ageRange = "AGE_RANGE"
        case dob = "DOB"
        case city = "CITY"
        case countryCode = "COUNTRY_CODE"
        case phoneNumber = "PHONE_NUMBER"
        case interests = "INTERESTS"
        case legalAgreement = "LEGAL_AGREEMENT"
    }
    public var nextStep: NextStep?
    public var link: String?
    public var fields: [Field]?
    public var missingFields: [MissingFields]?

    public init(nextStep: NextStep? = nil, link: String? = nil, fields: [Field]? = nil, missingFields: [MissingFields]? = nil) {
        self.nextStep = nextStep
        self.link = link
        self.fields = fields
        self.missingFields = missingFields
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case nextStep
        case link
        case fields
        case missingFields
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(nextStep, forKey: .nextStep)
        try container.encodeIfPresent(link, forKey: .link)
        try container.encodeIfPresent(fields, forKey: .fields)
        try container.encodeIfPresent(missingFields, forKey: .missingFields)
    }
}

