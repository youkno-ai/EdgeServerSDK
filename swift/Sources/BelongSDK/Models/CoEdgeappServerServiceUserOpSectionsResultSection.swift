//
// CoEdgeappServerServiceUserOpSectionsResultSection.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CoEdgeappServerServiceUserOpSectionsResultSection: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case topPick = "TOP_PICK"
        case individual = "INDIVIDUAL"
        case highlights = "HIGHLIGHTS"
        case uncompleted = "UNCOMPLETED"
        case tokenReward = "TOKEN_REWARD"
        case scheduled = "SCHEDULED"
        case expiration = "EXPIRATION"
        case new = "NEW"
        case adminsChoice = "ADMINS_CHOICE"
        case favourites = "FAVOURITES"
        case hotTopic = "HOT_TOPIC"
        case gem = "GEM"
        case express = "EXPRESS"
        case mood = "MOOD"
        case category = "CATEGORY"
        case all = "ALL"
    }
    public var type: ModelType?
    public var name: String?
    public var result: CoEdgeappServerServiceUserOpListResult?
    public var categoryCode: String?
    public var scheme: String?

    public init(type: ModelType? = nil, name: String? = nil, result: CoEdgeappServerServiceUserOpListResult? = nil, categoryCode: String? = nil, scheme: String? = nil) {
        self.type = type
        self.name = name
        self.result = result
        self.categoryCode = categoryCode
        self.scheme = scheme
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case name
        case result
        case categoryCode
        case scheme
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(type, forKey: .type)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(result, forKey: .result)
        try container.encodeIfPresent(categoryCode, forKey: .categoryCode)
        try container.encodeIfPresent(scheme, forKey: .scheme)
    }
}

