//
// UpdateUserResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct UpdateUserResult: Codable, JSONEncodable, Hashable {

    public enum UserStatus: String, Codable, CaseIterable {
        case deleted = "DELETED"
        case rejected = "REJECTED"
        case accepted = "ACCEPTED"
        case pending = "PENDING"
        case suspended = "SUSPENDED"
    }
    public enum NextStep: String, Codable, CaseIterable {
        case _default = "DEFAULT"
        case signup = "SIGNUP"
        case merchantOnboarding = "MERCHANT_ONBOARDING"
        case link = "LINK"
    }
    public enum MergeStatus: String, Codable, CaseIterable {
        case merged = "MERGED"
        case alreadyMerged = "ALREADY_MERGED"
    }
    public var userStatus: UserStatus?
    public var statusNote: String?
    public var nextStep: NextStep?
    public var link: String?
    public var mergeStatus: MergeStatus?

    public init(userStatus: UserStatus? = nil, statusNote: String? = nil, nextStep: NextStep? = nil, link: String? = nil, mergeStatus: MergeStatus? = nil) {
        self.userStatus = userStatus
        self.statusNote = statusNote
        self.nextStep = nextStep
        self.link = link
        self.mergeStatus = mergeStatus
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case userStatus
        case statusNote
        case nextStep
        case link
        case mergeStatus
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(userStatus, forKey: .userStatus)
        try container.encodeIfPresent(statusNote, forKey: .statusNote)
        try container.encodeIfPresent(nextStep, forKey: .nextStep)
        try container.encodeIfPresent(link, forKey: .link)
        try container.encodeIfPresent(mergeStatus, forKey: .mergeStatus)
    }
}

