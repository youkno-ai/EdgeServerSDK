//
// CoEdgeappServerAiBountyBountyGeneratorInput.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CoEdgeappServerAiBountyBountyGeneratorInput: Codable, JSONEncodable, Hashable {

    public enum BountyType: String, Codable, CaseIterable {
        case _none = "NONE"
        case question = "QUESTION"
        case thread = "THREAD"
        case talentSearch = "TALENT_SEARCH"
        case talentRecommendation = "TALENT_RECOMMENDATION"
        case tmobChallenge = "TMOB_CHALLENGE"
        case tmobCoaching = "TMOB_COACHING"
        case order = "ORDER"
        case realestateCustomer = "REALESTATE_CUSTOMER"
        case realestateRecommendation = "REALESTATE_RECOMMENDATION"
        case survey = "SURVEY"
        case classified = "CLASSIFIED"
        case product = "PRODUCT"
        case menu = "MENU"
        case bundle = "BUNDLE"
        case story = "STORY"
        case score = "SCORE"
        case series = "SERIES"
        case checklist = "CHECKLIST"
        case support = "SUPPORT"
        case aiBot = "AI_BOT"
        case deal = "DEAL"
        case promotion = "PROMOTION"
        case autosuggestion = "AUTOSUGGESTION"
        case approval = "APPROVAL"
        case mcq = "MCQ"
        case funding = "FUNDING"
        case auction = "AUCTION"
        case roleplay = "ROLEPLAY"
        case privateListing = "PRIVATE_LISTING"
        case quest = "QUEST"
        case liveStream = "LIVE_STREAM"
        case album = "ALBUM"
        case news = "NEWS"
        case banner = "BANNER"
        case match = "MATCH"
        case contest = "CONTEST"
        case trivia = "TRIVIA"
        case lottery = "LOTTERY"
        case badgeCollection = "BADGE_COLLECTION"
        case raffle = "RAFFLE"
        case calendarEvent = "CALENDAR_EVENT"
        case currency = "CURRENCY"
        case reservation = "RESERVATION"
        case quickClaim = "QUICK_CLAIM"
        case campaign = "CAMPAIGN"
        case expressCampaign = "EXPRESS_CAMPAIGN"
        case geoLocation = "GEO_LOCATION"
        case feedback = "FEEDBACK"
        case unknown = "UNKNOWN"
    }
    public var bountyType: BountyType?
    public var description: String?
    public var language: String?
    public var includeAttachments: Bool?
    public var deterministic: Bool?
    public var seed: Int64?

    public init(bountyType: BountyType? = nil, description: String? = nil, language: String? = nil, includeAttachments: Bool? = nil, deterministic: Bool? = nil, seed: Int64? = nil) {
        self.bountyType = bountyType
        self.description = description
        self.language = language
        self.includeAttachments = includeAttachments
        self.deterministic = deterministic
        self.seed = seed
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case bountyType
        case description
        case language
        case includeAttachments
        case deterministic
        case seed
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(bountyType, forKey: .bountyType)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encodeIfPresent(language, forKey: .language)
        try container.encodeIfPresent(includeAttachments, forKey: .includeAttachments)
        try container.encodeIfPresent(deterministic, forKey: .deterministic)
        try container.encodeIfPresent(seed, forKey: .seed)
    }
}

