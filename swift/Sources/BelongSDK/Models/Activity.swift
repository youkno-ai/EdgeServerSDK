//
// Activity.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct Activity: Codable, JSONEncodable, Hashable {

    public enum BountyTypeAsEnum: String, Codable, CaseIterable {
        case _none = "NONE"
        case question = "QUESTION"
        case thread = "THREAD"
        case talentSearch = "TALENT_SEARCH"
        case talentRecommendation = "TALENT_RECOMMENDATION"
        case tmobChallenge = "TMOB_CHALLENGE"
        case tmobCoaching = "TMOB_COACHING"
        case order = "ORDER"
        case realestateCustomer = "REALESTATE_CUSTOMER"
        case realestateRecommendation = "REALESTATE_RECOMMENDATION"
        case survey = "SURVEY"
        case classified = "CLASSIFIED"
        case product = "PRODUCT"
        case bundle = "BUNDLE"
        case story = "STORY"
        case score = "SCORE"
        case series = "SERIES"
        case checklist = "CHECKLIST"
        case support = "SUPPORT"
        case aiBot = "AI_BOT"
        case deal = "DEAL"
        case promotion = "PROMOTION"
        case autosuggestion = "AUTOSUGGESTION"
        case approval = "APPROVAL"
        case mcq = "MCQ"
        case funding = "FUNDING"
        case auction = "AUCTION"
        case roleplay = "ROLEPLAY"
        case privateListing = "PRIVATE_LISTING"
        case quest = "QUEST"
        case liveStream = "LIVE_STREAM"
        case album = "ALBUM"
        case news = "NEWS"
        case banner = "BANNER"
        case match = "MATCH"
        case contest = "CONTEST"
        case trivia = "TRIVIA"
        case lottery = "LOTTERY"
        case badgeCollection = "BADGE_COLLECTION"
        case raffle = "RAFFLE"
        case calendarEvent = "CALENDAR_EVENT"
        case currency = "CURRENCY"
        case reservation = "RESERVATION"
        case quickClaim = "QUICK_CLAIM"
        case campaign = "CAMPAIGN"
        case expressCampaign = "EXPRESS_CAMPAIGN"
        case geoLocation = "GEO_LOCATION"
        case feedback = "FEEDBACK"
        case unknown = "UNKNOWN"
    }
    public var bountyId: String?
    public var bountyType: String?
    public var bountyTitle: String?
    public var order: Int?
    public var bountyTypeAsEnum: BountyTypeAsEnum?

    public init(bountyId: String? = nil, bountyType: String? = nil, bountyTitle: String? = nil, order: Int? = nil, bountyTypeAsEnum: BountyTypeAsEnum? = nil) {
        self.bountyId = bountyId
        self.bountyType = bountyType
        self.bountyTitle = bountyTitle
        self.order = order
        self.bountyTypeAsEnum = bountyTypeAsEnum
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case bountyId
        case bountyType
        case bountyTitle
        case order
        case bountyTypeAsEnum
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(bountyId, forKey: .bountyId)
        try container.encodeIfPresent(bountyType, forKey: .bountyType)
        try container.encodeIfPresent(bountyTitle, forKey: .bountyTitle)
        try container.encodeIfPresent(order, forKey: .order)
        try container.encodeIfPresent(bountyTypeAsEnum, forKey: .bountyTypeAsEnum)
    }
}

