//
// BountySectionResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct BountySectionResult: Codable, JSONEncodable, Hashable {

    public enum Sections: String, Codable, CaseIterable {
        case topPick = "TOP_PICK"
        case individual = "INDIVIDUAL"
        case highlights = "HIGHLIGHTS"
        case uncompleted = "UNCOMPLETED"
        case tokenReward = "TOKEN_REWARD"
        case scheduled = "SCHEDULED"
        case expiration = "EXPIRATION"
        case new = "NEW"
        case adminsChoice = "ADMINS_CHOICE"
        case favourites = "FAVOURITES"
        case hotTopic = "HOT_TOPIC"
        case gem = "GEM"
        case express = "EXPRESS"
        case mood = "MOOD"
        case category = "CATEGORY"
        case all = "ALL"
    }
    public var sections: [Sections]?

    public init(sections: [Sections]? = nil) {
        self.sections = sections
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case sections
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(sections, forKey: .sections)
    }
}

