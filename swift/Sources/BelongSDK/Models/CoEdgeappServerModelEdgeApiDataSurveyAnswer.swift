//
// CoEdgeappServerModelEdgeApiDataSurveyAnswer.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CoEdgeappServerModelEdgeApiDataSurveyAnswer: Codable, JSONEncodable, Hashable {

    public enum ResponseState: String, Codable, CaseIterable {
        case _none = "NONE"
        case draft = "DRAFT"
        case approval = "APPROVAL"
        case pending = "PENDING"
        case active = "ACTIVE"
        case acked = "ACKED"
        case progressing = "PROGRESSING"
        case accepted = "ACCEPTED"
        case refused = "REFUSED"
        case paying = "PAYING"
        case paid = "PAID"
        case retracted = "RETRACTED"
        case rejected = "REJECTED"
        case deleted = "DELETED"
        case unknown = "UNKNOWN"
    }
    public var bountyId: String?
    public var answers: CoEdgeappServerModelBountyResponseSurveyAnswers?
    public var responseId: String?
    public var responseState: ResponseState?
    public var reward: CoEdgeappServerModelReward?

    public init(bountyId: String? = nil, answers: CoEdgeappServerModelBountyResponseSurveyAnswers? = nil, responseId: String? = nil, responseState: ResponseState? = nil, reward: CoEdgeappServerModelReward? = nil) {
        self.bountyId = bountyId
        self.answers = answers
        self.responseId = responseId
        self.responseState = responseState
        self.reward = reward
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case bountyId
        case answers
        case responseId
        case responseState
        case reward
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(bountyId, forKey: .bountyId)
        try container.encodeIfPresent(answers, forKey: .answers)
        try container.encodeIfPresent(responseId, forKey: .responseId)
        try container.encodeIfPresent(responseState, forKey: .responseState)
        try container.encodeIfPresent(reward, forKey: .reward)
    }
}

