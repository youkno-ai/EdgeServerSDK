//
// ExplainSelfLinkResp.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ExplainSelfLinkResp: Codable, JSONEncodable, Hashable {

    public enum SelfLinkType: String, Codable, CaseIterable {
        case _none = "NONE"
        case voucher = "VOUCHER"
        case unknown = "UNKNOWN"
    }
    public enum EntityType: String, Codable, CaseIterable {
        case _none = "NONE"
        case unknown = "UNKNOWN"
        case user = "USER"
        case bounty = "BOUNTY"
        case response = "RESPONSE"
        case comment = "COMMENT"
        case chat = "CHAT"
        case message = "MESSAGE"
        case attachment = "ATTACHMENT"
        case pushNotification = "PUSH_NOTIFICATION"
        case address = "ADDRESS"
        case seat = "SEAT"
        case event = "EVENT"
        case venue = "VENUE"
        case ticket = "TICKET"
        case category = "CATEGORY"
        case system = "SYSTEM"
    }
    public var selfLinkType: SelfLinkType?
    public var entityType: EntityType?
    public var entityId: String?

    public init(selfLinkType: SelfLinkType? = nil, entityType: EntityType? = nil, entityId: String? = nil) {
        self.selfLinkType = selfLinkType
        self.entityType = entityType
        self.entityId = entityId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case selfLinkType
        case entityType
        case entityId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(selfLinkType, forKey: .selfLinkType)
        try container.encodeIfPresent(entityType, forKey: .entityType)
        try container.encodeIfPresent(entityId, forKey: .entityId)
    }
}

