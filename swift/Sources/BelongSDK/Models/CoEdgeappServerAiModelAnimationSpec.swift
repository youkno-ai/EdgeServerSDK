//
// CoEdgeappServerAiModelAnimationSpec.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CoEdgeappServerAiModelAnimationSpec: Codable, JSONEncodable, Hashable {

    public enum Style: String, Codable, CaseIterable {
        case flatIcon = "FLAT_ICON"
        case lineArt = "LINE_ART"
        case gradientIcon = "GRADIENT_ICON"
        case glowBadge = "GLOW_BADGE"
        case cartoon = "CARTOON"
        case monochrome = "MONOCHROME"
    }
    public enum Loop: String, Codable, CaseIterable {
        case spin = "SPIN"
        case pulse = "PULSE"
        case bounce = "BOUNCE"
        case float = "FLOAT"
        case wobble = "WOBBLE"
        case flash = "FLASH"
    }
    public var style: Style?
    public var loop: Loop?

    public init(style: Style? = nil, loop: Loop? = nil) {
        self.style = style
        self.loop = loop
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case style
        case loop
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(style, forKey: .style)
        try container.encodeIfPresent(loop, forKey: .loop)
    }
}

