//
// UpdateBountyStateReq.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct UpdateBountyStateReq: Codable, JSONEncodable, Hashable {

    public enum NewState: String, Codable, CaseIterable {
        case _none = "NONE"
        case draft = "DRAFT"
        case pending = "PENDING"
        case generating = "GENERATING"
        case active = "ACTIVE"
        case sealed = "SEALED"
        case paused = "PAUSED"
        case closed = "CLOSED"
        case expired = "EXPIRED"
        case retracted = "RETRACTED"
        case rejected = "REJECTED"
        case archived = "ARCHIVED"
        case deleted = "DELETED"
        case unknown = "UNKNOWN"
    }
    public var newState: NewState?

    public init(newState: NewState? = nil) {
        self.newState = newState
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case newState
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(newState, forKey: .newState)
    }
}

