//
// CoEdgeappServerRestControllerOpenControllerInviteInfoResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CoEdgeappServerRestControllerOpenControllerInviteInfoResult: Codable, JSONEncodable, Hashable {

    public enum InviteStatus: String, Codable, CaseIterable {
        case pending = "PENDING"
        case canceled = "CANCELED"
        case deleted = "DELETED"
        case voided = "VOIDED"
        case accepted = "ACCEPTED"
        case declined = "DECLINED"
        case unknown = "UNKNOWN"
    }
    public enum Logins: String, Codable, CaseIterable {
        case _none = "NONE"
        case sso = "SSO"
        case google = "GOOGLE"
        case apple = "APPLE"
        case microsoft = "MICROSOFT"
        case email = "EMAIL"
        case phone = "PHONE"
        case internet = "INTERNET"
        case anon = "ANON"
        case unknown = "UNKNOWN"
    }
    public var inviteStatus: InviteStatus?
    public var company: CoEdgeappServerModelUser?
    public var inviter: CoEdgeappServerModelUser?
    public var clientId: String?
    public var logins: [Logins]?
    public var name: String?
    public var email: String?
    public var phone: String?
    public var roles: [String]?

    public init(inviteStatus: InviteStatus? = nil, company: CoEdgeappServerModelUser? = nil, inviter: CoEdgeappServerModelUser? = nil, clientId: String? = nil, logins: [Logins]? = nil, name: String? = nil, email: String? = nil, phone: String? = nil, roles: [String]? = nil) {
        self.inviteStatus = inviteStatus
        self.company = company
        self.inviter = inviter
        self.clientId = clientId
        self.logins = logins
        self.name = name
        self.email = email
        self.phone = phone
        self.roles = roles
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case inviteStatus
        case company
        case inviter
        case clientId
        case logins
        case name
        case email
        case phone
        case roles
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(inviteStatus, forKey: .inviteStatus)
        try container.encodeIfPresent(company, forKey: .company)
        try container.encodeIfPresent(inviter, forKey: .inviter)
        try container.encodeIfPresent(clientId, forKey: .clientId)
        try container.encodeIfPresent(logins, forKey: .logins)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(email, forKey: .email)
        try container.encodeIfPresent(phone, forKey: .phone)
        try container.encodeIfPresent(roles, forKey: .roles)
    }
}

