//
// CoEdgeappServerRestControllerAIControllerBountyGenerationStatus.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CoEdgeappServerRestControllerAIControllerBountyGenerationStatus: Codable, JSONEncodable, Hashable {

    public enum Phase: String, Codable, CaseIterable {
        case instructions = "INSTRUCTIONS"
        case finalizing = "FINALIZING"
    }
    public enum BountyType: String, Codable, CaseIterable {
        case _none = "NONE"
        case question = "QUESTION"
        case thread = "THREAD"
        case talentSearch = "TALENT_SEARCH"
        case talentRecommendation = "TALENT_RECOMMENDATION"
        case tmobChallenge = "TMOB_CHALLENGE"
        case tmobCoaching = "TMOB_COACHING"
        case order = "ORDER"
        case realestateCustomer = "REALESTATE_CUSTOMER"
        case realestateRecommendation = "REALESTATE_RECOMMENDATION"
        case survey = "SURVEY"
        case classified = "CLASSIFIED"
        case product = "PRODUCT"
        case menu = "MENU"
        case bundle = "BUNDLE"
        case story = "STORY"
        case score = "SCORE"
        case series = "SERIES"
        case checklist = "CHECKLIST"
        case support = "SUPPORT"
        case aiBot = "AI_BOT"
        case deal = "DEAL"
        case promotion = "PROMOTION"
        case autosuggestion = "AUTOSUGGESTION"
        case approval = "APPROVAL"
        case mcq = "MCQ"
        case funding = "FUNDING"
        case auction = "AUCTION"
        case roleplay = "ROLEPLAY"
        case privateListing = "PRIVATE_LISTING"
        case quest = "QUEST"
        case liveStream = "LIVE_STREAM"
        case album = "ALBUM"
        case news = "NEWS"
        case banner = "BANNER"
        case match = "MATCH"
        case contest = "CONTEST"
        case trivia = "TRIVIA"
        case lottery = "LOTTERY"
        case badgeCollection = "BADGE_COLLECTION"
        case raffle = "RAFFLE"
        case calendarEvent = "CALENDAR_EVENT"
        case currency = "CURRENCY"
        case reservation = "RESERVATION"
        case quickClaim = "QUICK_CLAIM"
        case campaign = "CAMPAIGN"
        case expressCampaign = "EXPRESS_CAMPAIGN"
        case geoLocation = "GEO_LOCATION"
        case feedback = "FEEDBACK"
        case unknown = "UNKNOWN"
    }
    public var jobId: UUID?
    public var companyId: String?
    public var phase: Phase?
    public var progressDoneUnits: Int?
    public var progressTotalUnits: Int?
    public var bountyType: BountyType?
    public var bountyId: String?

    public init(jobId: UUID? = nil, companyId: String? = nil, phase: Phase? = nil, progressDoneUnits: Int? = nil, progressTotalUnits: Int? = nil, bountyType: BountyType? = nil, bountyId: String? = nil) {
        self.jobId = jobId
        self.companyId = companyId
        self.phase = phase
        self.progressDoneUnits = progressDoneUnits
        self.progressTotalUnits = progressTotalUnits
        self.bountyType = bountyType
        self.bountyId = bountyId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case jobId
        case companyId
        case phase
        case progressDoneUnits
        case progressTotalUnits
        case bountyType
        case bountyId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(jobId, forKey: .jobId)
        try container.encodeIfPresent(companyId, forKey: .companyId)
        try container.encodeIfPresent(phase, forKey: .phase)
        try container.encodeIfPresent(progressDoneUnits, forKey: .progressDoneUnits)
        try container.encodeIfPresent(progressTotalUnits, forKey: .progressTotalUnits)
        try container.encodeIfPresent(bountyType, forKey: .bountyType)
        try container.encodeIfPresent(bountyId, forKey: .bountyId)
    }
}

