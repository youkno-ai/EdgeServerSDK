//
// PaymentSummaryResp.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct PaymentSummaryResp: Codable, JSONEncodable, Hashable {

    public enum PaymentStatus: String, Codable, CaseIterable {
        case _open = "OPEN"
        case charging = "CHARGING"
        case processing = "PROCESSING"
        case succeeded = "SUCCEEDED"
        case failed = "FAILED"
        case canceled = "CANCELED"
    }
    public enum ChargeStatus: String, Codable, CaseIterable {
        case _none = "NONE"
        case requiresPaymentMethod = "REQUIRES_PAYMENT_METHOD"
        case requiresConfirmation = "REQUIRES_CONFIRMATION"
        case requiresAction = "REQUIRES_ACTION"
        case processing = "PROCESSING"
        case requiresCapture = "REQUIRES_CAPTURE"
        case canceled = "CANCELED"
        case succeeded = "SUCCEEDED"
        case failed = "FAILED"
        case unknown = "UNKNOWN"
    }
    public var liveMode: Bool?
    public var paymentStatus: PaymentStatus?
    public var chargeStatus: ChargeStatus?

    public init(liveMode: Bool? = nil, paymentStatus: PaymentStatus? = nil, chargeStatus: ChargeStatus? = nil) {
        self.liveMode = liveMode
        self.paymentStatus = paymentStatus
        self.chargeStatus = chargeStatus
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case liveMode
        case paymentStatus
        case chargeStatus
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(liveMode, forKey: .liveMode)
        try container.encodeIfPresent(paymentStatus, forKey: .paymentStatus)
        try container.encodeIfPresent(chargeStatus, forKey: .chargeStatus)
    }
}

