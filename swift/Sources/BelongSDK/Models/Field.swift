//
// Field.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct Field: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case name = "NAME"
        case avatar = "AVATAR"
        case gender = "GENDER"
        case ageRange = "AGE_RANGE"
        case dob = "DOB"
        case city = "CITY"
        case countryCode = "COUNTRY_CODE"
        case phoneNumber = "PHONE_NUMBER"
        case interests = "INTERESTS"
        case legalAgreement = "LEGAL_AGREEMENT"
    }
    public enum Status: String, Codable, CaseIterable {
        case _required = "REQUIRED"
        case _optional = "OPTIONAL"
    }
    public var type: ModelType?
    public var status: Status?
    public var currentValue: AnyCodable?

    public init(type: ModelType? = nil, status: Status? = nil, currentValue: AnyCodable? = nil) {
        self.type = type
        self.status = status
        self.currentValue = currentValue
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case status
        case currentValue
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(type, forKey: .type)
        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(currentValue, forKey: .currentValue)
    }
}

