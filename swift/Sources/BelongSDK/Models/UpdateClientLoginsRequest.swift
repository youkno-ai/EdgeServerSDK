//
// UpdateClientLoginsRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct UpdateClientLoginsRequest: Codable, JSONEncodable, Hashable {

    public enum Logins: String, Codable, CaseIterable {
        case _none = "NONE"
        case sso = "SSO"
        case google = "GOOGLE"
        case apple = "APPLE"
        case microsoft = "MICROSOFT"
        case email = "EMAIL"
        case phone = "PHONE"
        case internet = "INTERNET"
        case anon = "ANON"
        case unknown = "UNKNOWN"
    }
    public var logins: [Logins]?

    public init(logins: [Logins]? = nil) {
        self.logins = logins
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case logins
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(logins, forKey: .logins)
    }
}

