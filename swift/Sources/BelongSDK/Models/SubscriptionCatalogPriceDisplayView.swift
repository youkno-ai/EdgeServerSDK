//
// SubscriptionCatalogPriceDisplayView.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct SubscriptionCatalogPriceDisplayView: Codable, JSONEncodable, Hashable {

    public enum CadenceOptions: String, Codable, CaseIterable {
        case monthly = "MONTHLY"
        case yearly = "YEARLY"
        case unknown = "UNKNOWN"
    }
    public enum SelectedCadence: String, Codable, CaseIterable {
        case monthly = "MONTHLY"
        case yearly = "YEARLY"
        case unknown = "UNKNOWN"
    }
    public var currencyCode: String?
    public var cadenceOptions: [CadenceOptions]?
    public var selectedCadence: SelectedCadence?

    public init(currencyCode: String? = nil, cadenceOptions: [CadenceOptions]? = nil, selectedCadence: SelectedCadence? = nil) {
        self.currencyCode = currencyCode
        self.cadenceOptions = cadenceOptions
        self.selectedCadence = selectedCadence
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case currencyCode
        case cadenceOptions
        case selectedCadence
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(currencyCode, forKey: .currencyCode)
        try container.encodeIfPresent(cadenceOptions, forKey: .cadenceOptions)
        try container.encodeIfPresent(selectedCadence, forKey: .selectedCadence)
    }
}

