//
// InviteInfoResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct InviteInfoResult: Codable, JSONEncodable, Hashable {

    public enum InviteStatus: String, Codable, CaseIterable {
        case pending = "PENDING"
        case canceled = "CANCELED"
        case deleted = "DELETED"
        case voided = "VOIDED"
        case accepted = "ACCEPTED"
        case declined = "DECLINED"
        case unknown = "UNKNOWN"
    }
    public enum Logins: String, Codable, CaseIterable {
        case _none = "NONE"
        case sso = "SSO"
        case google = "GOOGLE"
        case apple = "APPLE"
        case microsoft = "MICROSOFT"
        case email = "EMAIL"
        case phone = "PHONE"
        case internet = "INTERNET"
        case anon = "ANON"
        case unknown = "UNKNOWN"
    }
    public var inviteStatus: InviteStatus?
    public var clientId: String?
    public var logins: [Logins]?
    public var name: String?
    public var email: String?
    public var phone: String?

    public init(inviteStatus: InviteStatus? = nil, clientId: String? = nil, logins: [Logins]? = nil, name: String? = nil, email: String? = nil, phone: String? = nil) {
        self.inviteStatus = inviteStatus
        self.clientId = clientId
        self.logins = logins
        self.name = name
        self.email = email
        self.phone = phone
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case inviteStatus
        case clientId
        case logins
        case name
        case email
        case phone
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(inviteStatus, forKey: .inviteStatus)
        try container.encodeIfPresent(clientId, forKey: .clientId)
        try container.encodeIfPresent(logins, forKey: .logins)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(email, forKey: .email)
        try container.encodeIfPresent(phone, forKey: .phone)
    }
}

