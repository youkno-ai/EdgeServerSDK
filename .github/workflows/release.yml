name: Release SDKs

on:
  push:
    tags:
      - 'sdk-*'

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish-ts:
    name: Publish TypeScript SDK
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
          cache: npm
          cache-dependency-path: ts/package-lock.json

      - name: Build and publish npm package
        working-directory: ts
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          npm ci
          npm run build
          npm publish --access public

  publish-kotlin:
    name: Publish Kotlin SDK
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Publish to Maven Central
        working-directory: kotlin
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.SONATYPE_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.SONATYPE_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.SIGNING_KEY }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.SIGNING_PASSWORD }}
        run: ./gradlew --no-daemon -Dkotlin.compiler.execution.strategy=in-process publishAllPublicationsToMavenCentralRepository

  validate-swift:
    name: Validate Swift Package
    runs-on: macos-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Swift package
        working-directory: swift
        run: swift build

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs:
      - publish-ts
      - publish-kotlin
      - validate-swift
    permissions:
      contents: write
    steps:
      - name: Extract version from sdk tag
        id: version
        run: echo "value=${GITHUB_REF_NAME#sdk-}" >> "$GITHUB_OUTPUT"

      - name: Create release with autogenerated notes
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.value }}
          target_commitish: ${{ github.sha }}
          name: SDK ${{ steps.version.outputs.value }}
          generate_release_notes: true
          make_latest: true
